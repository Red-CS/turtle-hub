// Constants for the main and renderer Processes

// For toggling the server
const TOGGLE_SERVER: string = "toggle-server";

// For returning the state of the server
const SERVER_STATUS: string = "server-status";

export { TOGGLE_SERVER, SERVER_STATUS };
